Not accepting Contributions yet since it's very early stage of development.

# NEXT

4. What about zod form validation instead of the errors. and do not it have validation by default? like html default validation.
5. email regex and other validations?
6. run backend and frontend with single command
7. Connect login backend with frontend
8. Why use local storage for token instead of cookies?
9. Break things into components - like signup buttons. instead of just being on home page, they can be added to other places like navbar and unauthorized pages.
10. how and where (monorepo) to add the mcps like shadcn
11. Resolve the different student/teacher pages/routes.
12. logout should redirect to home page.
