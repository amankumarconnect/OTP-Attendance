Not accepting Contributions yet since it's very early stage of development.

# NEXT

1. What if we only send token
2. What if we do not extend for role and use props.role
3. Test the loading, error etc.
4. What about zod form validation instead of the errors. and do not it have validation by default? like html default validation.
5. is the isauthenticated variable needed?
6. FormEvent is deprecated
7. why use response data for login function arguments instead of input variables?
8. why not user object/type instead of multiple seperate input variables?
9. use Link instead of a tag in signup form
10. Is shadcn for react only?
11. In signup form component, pass role props in lowercase already?
12. email regex and other validations?
